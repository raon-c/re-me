// Type definitions will be implemented in later tasks
export interface User {
  id: string;
  email: string;
  name: string;
}

export interface Invitation {
  id: string;
  title: string;
  // More fields will be added in later tasks
}
