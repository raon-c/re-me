// Authentication hook will be implemented in later tasks
export const useAuth = () => {
  // Placeholder for authentication hook
  return {
    user: null,
    loading: false,
    error: null,
  };
};
